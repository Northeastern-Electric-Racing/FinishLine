import { AuthenticatedUser } from 'shared';

interface LeadFinanceDashboardProps {
  user: AuthenticatedUser;
}

const LeadFinanceDashboard = ({ user }: LeadFinanceDashboardProps) => {};

export default LeadFinanceDashboard;
